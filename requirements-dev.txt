pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
httpx>=0.27          # async test client for FastAPI
factory-boy>=3.3     # test data factories
freezegun>=1.4       # time freezing for date tests
moto[s3]>=5.0        # mock AWS S3
responses>=0.25      # mock HTTP responses (for external APIs)
